<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mapper.UserMapper">

	<!--<select id="getAllTeacherIdsByparentId" resultType="com.eclassedu.pojo.WsUser" parameterType="java.lang.Long">-->
		<!--select * from t_user where username = ${name} and password = {pwd};-->
	<!--</select>-->
	<select id="findByName" resultType="com.test.entity.User"></select>
</mapper>
